const baseUrl = "http://127.0.0.1:5000/SP";
export const url = {
  service: `${baseUrl}/service`,
  serviceRequest: `${baseUrl}/serviceRequest`,
  station: `${baseUrl}/station`,
  register: `${baseUrl}/register`,
  auth: `${baseUrl}/auth`,
  fuelServiceRequest: `${baseUrl}/fuelServiceRequest`,
};
